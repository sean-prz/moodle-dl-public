async function main() {
    // runServer()
    // console.log("Welcome to Moodle PDF Downloader");
    //await new Promise(resolve => setTimeout(resolve, 1000));
    // three next lines are for terminal interaction
    //await checkValidSettings(); // Check if cookie and at least one course are set.
    //await changeSettings();
    // downloadAll();
}

